DJANGO_SECRET_KEY=replace-with-a-strong-secret-key
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# For local demos, the app falls back to SQLite if DATABASE_URL is omitted.
# PostgreSQL example:
# DATABASE_URL=postgres://employee_user:employee_password@localhost:5432/employee_project
